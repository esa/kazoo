Demo_ContextParams

Tests the Context Parameters feature of the toolchain, in C and Ada functions.

This feature allows the user to specify some context-dependent properties in functions of the interface view.
