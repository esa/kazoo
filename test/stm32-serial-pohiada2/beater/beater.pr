/* CIF PROCESS (250, 150), (150, 75) */
process beater;
    /* CIF TEXT (740, 47), (272, 140) */
    -- Text area for declarations and comments
    
    DCL stamp T_Int32;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF task (313, 65), (84, 35) */
        task stamp := 0;
        /* CIF NEXTSTATE (320, 115), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (524, 67), (70, 35) */
    state Wait;
    endstate;
endprocess beater;