/* CIF PROCESS (250, 150), (150, 75) */
process Obsw;
    /* CIF TEXT (504, 123), (270, 153) */
    -- Text area for declarations and comments
    
    dcl p t_boolean;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (320, 60), (70, 35) */
    state Wait;
        /* CIF input (316, 115), (78, 35) */
        input Run(p);
            /* CIF output (313, 170), (83, 35) */
            output Result(42);
            /* CIF NEXTSTATE (320, 220), (70, 35) */
            NEXTSTATE wait;
    endstate;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
    endstate;
endprocess Obsw;